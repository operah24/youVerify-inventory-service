import { createItem } from "./createItem";
import { getItem } from "./getItem";
import { addStock, deductStock } from "./updateStock";

export default {
    createItem,
    addStock,
    getItem,
    deductStock
    
}